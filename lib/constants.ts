// MVP: um único evento. Trocar por resolução via subdomínio/rota dinâmica
// quando o produto virar multi-evento — nenhuma RPC/RLS depende deste
// valor estar hardcoded, então essa troca não exige mudança no banco.
export const DEFAULT_EVENT_SLUG = 'rafaely-e-vitor'
